import * as React from 'react';
import { useState } from 'react';
// Existing code in createRoles.tsx

// const CreateRolesCompponent;
const CreateRolesComponent: React.FC = () => {
    return (
        <div>Create Roles Component</div>
    );
};



// Add a default export

export default CreateRolesComponent;
